export interface IProduct {
    _id?: string,
    name: string,
    price: number,
    original_price: number,
    description?: string,
    image?: string,
    longDescription?: string,
    categoryId?: string,

}